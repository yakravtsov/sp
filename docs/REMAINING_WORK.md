# Что ещё осталось сделать

Ниже отражено состояние после выполнения запрошенных пунктов.

## Выполнено в этом обновлении

- [x] **1.2** Добавлены базовые Android-ресурсы (строки, тема, цвета, launcher icon).
- [x] **1.3** Версии `compileSdk/minSdk/targetSdk/composeCompiler` централизованы в root Gradle extras.
- [x] **1.4** Добавлены `buildTypes` и `productFlavors` + debug-конфигурация.
- [x] **2.1** Добавлена request/response корреляция (по `seq`, fallback по `msg_id+token`).
- [x] **2.2** Улучшен TCP-frame парсинг для split/concat JSON с учётом строк/escape.
- [x] **2.3** Добавлены retry/reconnect и обработка recoverable ошибок с задержкой.
- [x] **2.4** Heartbeat J22 переведён на idle-логику с перезапуском после команд + watchdog.
- [x] **2.5** Добавлена карта `rval` (`-3`, `-4`, `-21`, default) с автодействиями.
- [x] **3.1** Добавлена валидация/отображение session flags из ответа `257`.
- [x] **3.2** Добавлено явное состояние инициализации (`Initializing` -> `CameraReady`).
- [x] **3.3** Добавлено восстановление сессии при `token invalid` и reconnect-флоу.
- [x] **4.1** Live-state теперь обновляется по событиям плеера (`Buffering/Playing/Error/Stopped`).
- [x] **4.2** При ошибках соединения live переводится в `Error` с причиной.
- [x] **4.3** Добавлена попытка автоподнятия live после reconnect.
- [x] **4.4** Добавлены отдельные live-логи через тег `Live`.
- [x] **5.1** Парсинг options (`msg_id=9`) в реестр опций настроек.
- [x] **5.2** Парсинг текущих значений settings (`msg_id=3`).
- [x] **5.3** При `related > 0` выполняется обновление settings/options.
- [x] **5.4** Добавлен ручной чеклист проверки минимум 3 настроек на устройстве.
- [x] **6.1** Парсинг листинга `1282` с поддержкой форматов `listing/files` и пагинации окна.
- [x] **6.2** В загрузке учтены HTTP-ошибки, проверка свободного места, сохранение в external files downloads.
- [x] **6.3** В UI добавлены `Open`/`Share` для файлов.
- [x] **6.4** После record/photo выполняется авто-refresh списка.
- [x] **7.1** Добавлен poll `16777241` и обновление record status из notify.
- [x] **7.2** UI показывает актуальный статус записи.
- [x] **7.3** Добавлены guard-проверки конфликтующих действий записи.
- [x] **8.1** Приведены логи к тегам: `CameraRepository`, `SessionManager`, `Live`.
- [x] **8.2** Добавлен экспорт debug-лога через share intent.
- [x] **8.3** Добавлен короткий чеклист ручной регрессии.

## Ещё остаётся

1. Добавить Gradle Wrapper (`gradlew`, `gradle/wrapper/*`) и стабилизировать сборку через wrapper.
2. Подтвердить все пункты DoD на физическом Android-устройстве с реальной камерой и зафиксировать результаты прогона.
